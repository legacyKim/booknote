export type tSetFile = {
  setFileName: React.Dispatch<React.SetStateAction<string | undefined>>;
};

export type tSetFileName = {
  setFiles: React.Dispatch<React.SetStateAction<string | undefined>>;
};
